{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for Agent Limit.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
